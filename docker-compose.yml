version: "3.9"

services:
  forgebyteslab:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4174:4174"
    environment:
      NODE_ENV: production
    restart: unless-stopped
